<div class="schedule-details">
  <h2 class="mt-3">Schedule ID: <%= @schedule.id %></h2>
  <p><strong>作品ID:</strong> <%= @schedule.movie_id %></p>
  <p><strong>作品名:</strong> <%= @schedule.movie.name %></p>
  <p><strong>開始時刻:</strong> <%= l(@schedule.start_time) %></p>
  <p><strong>終了時刻:</strong> <%= l(@schedule.end_time) %></p>
  <p><strong>作成日時:</strong> <%= l(@schedule.created_at) %></p>
  <p><strong>更新日時:</strong> <%= l(@schedule.updated_at) %></p>

  <div class="mt-4">
    <%= link_to 'スケジュール一覧', admin_schedules_path, class: 'btn btn-secondary' %>
  </div>
</div>
