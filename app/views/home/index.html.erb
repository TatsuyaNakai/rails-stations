<div class="container mt-5">
  <h1 class="text-center mb-4">週間映画ランキング</h1>
  <div class="row">
    <% @ranking_count.each do |elm| %>
      <% movie = elm.movie %>
      <% reservation_count = elm.total_reservations %>
      <div class="col-md-6 mb-4">
        <div class="card h-100 shadow-sm">
          <div class="card-header bg-white d-flex justify-content-between align-items-center">
            <div>
              <h2 class="h5 mb-0"><%= link_to movie.name, movie_path(movie) %></h2>
              <p class="mb-0 text-secondary">過去30日分の予約数</p>
            </div>
            <h2 class="h5 mb-0"><%= reservation_count.to_s(:delimited) %> reservations!</h2>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
